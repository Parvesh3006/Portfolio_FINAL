# Mobile optimization TODO

- [ ] Add global/mobile overflow safety if needed (check index.html + global CSS)
- [x] Update HeroSection typography and vertical padding for mobile
- [x] Fix ExperienceSection timeline layout for mobile (responsive paddings/absolute positions, reduce card padding)

- [x] Reduce section vertical padding (py-32) and heading sizes (text-5xl) across all section components
- [x] Ensure any heavy cards/grids (About, Achievements, Research, Projects, TechStack, StatsSlider, Contact, Footer) fit on small screens without overflow
- [ ] Run `npm run build` to confirm no TS/CSS issues
- [ ] Run `npm run dev` (if needed) and do a quick manual mobile viewport sanity check

